apiVersion: agentlessscanning.datadog.upbound.io/v1alpha1
kind: AWSScanOptions
metadata:
  annotations:
    meta.upbound.io/example-id: agentlessscanning/v1alpha1/awsscanoptions
    upjet.upbound.io/manual-intervention: "Requires the id of an AWS account already integrated with Datadog (via the AWS integration)."
  labels:
    testing.upbound.io/example-name: example
  name: example
spec:
  forProvider:
    awsAccountId: "123456789012"
    lambda: true
    sensitiveData: false
    vulnContainersOs: true
    vulnHostOs: true
